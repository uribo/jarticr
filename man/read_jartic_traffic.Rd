% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_jartic_traffic.R
\name{read_jartic_traffic}
\alias{read_jartic_traffic}
\title{Read a JARTIC type B traffic file}
\usage{
read_jartic_traffic(path)
}
\arguments{
\item{path}{Path to a raw type B file (CSV format).}
}
\value{
A \code{data.table} of 10 columns, keyed on \code{datetime}:
\describe{
\item{datetime}{Observation time (\code{POSIXct}, \code{Asia/Tokyo}).}
\item{source_code}{Information source code.}
\item{location_no}{Observation location number.}
\item{location_name}{Observation location, as \code{from}\out{\U{2192}}\code{to}.}
\item{meshcode10km}{10 km mesh code.}
\item{link_type}{Link type.}
\item{link_no}{Link number.}
\item{traffic}{Traffic volume; missing observations are \code{NA}.}
\item{to_link_end_10m}{Distance to the link end, in units of 10 m. Kept as
character so that whatever the source writes -- padding included --
survives the read.}
\item{link_ver}{Link version; \code{NA} for files published before 2018-02.}
}
}
\description{
Reads one cross-sectional traffic volume ("type B") CSV file published by
the Japan Road Traffic Information Center. The files are CP932-encoded and
carry a header row; this function drops that row, supplies stable column
names, parses \code{datetime} in JST, and normalizes \code{location_name} with NFKC.
}
\details{
The published layout has changed over time, so the header row is inspected
before the file is read:
\itemize{
\item Files from 2018-02 onwards have 10 columns. Earlier files have 9 and no
\code{link_ver}; for those, \code{link_ver} is filled with \code{NA} so that the returned
columns are the same in either case.
\item A file whose first row is already an observation (no header) is read in
full; the first row is not consumed as a header.
\item \code{datetime} is written as \verb{2026/06/01 00:00} by most providers, but the
pre-2018-02 files also use \verb{2017/8/1 0:00:00}. Both are parsed, and values
that survive as \code{NA} are reported with a warning rather than passed on
silently.
}

Bytes that are not valid CP932 cannot be decoded and leave \code{NA} in
\code{location_name}. That is reported with a warning too, because the rest of
the row still looks intact.
}
\examples{
read_jartic_traffic(
  system.file("dummy", "type_b.csv", package = "jarticr")
)
}
